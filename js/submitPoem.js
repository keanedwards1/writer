/* document.getElementById("submit-button").addEventListener("click", function() {
    // Get the value of the poem input field.
    var poem = document.getElementById("poem-input").value;

    // Create a new request object.
    var request = new XMLHttpRequest();

    // Set the request method to "post".
    request.method = "post";

    // Set the request body to the value of the poem input field.
    request.body = poem;

    // Set the request destination to the value of the "page" input field.
    request.destination = "upvote.html";

    // Send the request.
    request.send();

    // If the request is successful, you will need to redirect the user to the upvote.html page.
    if (request.status === 200) {
        window.location = "upvote.html";
    }
}); */
